[project]
name = "curved-rope"
version = "0.1.0"
description = "Research prototypes for RoPE-like query/key transformations in non-Euclidean geometries."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

